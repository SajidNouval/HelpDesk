document.addEventListener('DOMContentLoaded', () => {
    const userForm = document.querySelector('[data-confirm]');
    if (!userForm) {
        return;
    }

    // Forms with data-confirm are handled globally in ui.js.
});